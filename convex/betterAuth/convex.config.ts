// Phase 3 — Stream 3A: Component definition for Better Auth.
//
// `defineComponent("betterAuth")` registers this directory as a
// locally-installed Convex component. The name `betterAuth` is the
// key used in `components.betterAuth` throughout the app.

import { defineComponent } from "convex/server";

const component = defineComponent("betterAuth");

export default component;
